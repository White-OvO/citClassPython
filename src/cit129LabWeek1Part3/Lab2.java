package cit129LabWeek1Part3;

public class Lab2 {

	public static void main(String[] args) {
		
		System.out.println("Truth or flase : "+ " lab 2");
		if(8 == 8 || 9 < 9 ) 
			System.out.println("True");
		if (3 < 5 && 9 > 7 ) {
			System.out.println("This is also true for the first equation");
			}
		else{ System.out.println("This is false");
		}
		if (4!= 6 && 4 > 7) { 
			System.out.println("This is true"  );
		}else { 
			System.out.println("Sorry the third equation is false");
		}
		
		if ( 6 == 5  || 6!=6) { 
			System.out.println("This is true");
		}else 
		{
			System.out.println("Try again on the last equation");
		}
		
		
/*2 If Num1 = 5 and Num2 = 4, determine whether each of the following expressions is true or false.
a. (Num1 == 1) OR (Num2 == 2) AND (Num1 == Num2)
b. ((Num1 == 1) OR (Num2 == 2)) AND (Num1 == Num2)
c. NOT (Num1 == 1) AND NOT (Num2 == 2) d. NOT (Num1 == 1) OR NOT (Num2 == 2)

		 */
		int num1 = 5;
		int num2 = 4;
		
		if ((num1 == 1)||(num2 == 2) && (num1 == num2)) { 
			System.out.println("This will for the first one ");
			
		}
		else System.out.println("Something needs to be changed ");
			

	
	if((num1 == 1)|| (num2 == 2) && (num1 == num2)){ 
		System.out.println("This will for the Second equation ");
		
	}
	else System.out.println("Something needs to be changed in the second equation: ");
	}	
	
	
	/*NOT (Num1 == 1) AND NOT (Num2 == 2)
	 * 
	 * 
	 */
	
	
///////////////////////////////////////////////////////////////////////////////////////
//pseuedocdode 

// variables : itemName, salePrice, Tax, TotalPrice

//salePrice = originalPrice - AmountSaved
//AmountSaved = originalPrice * (DiscountRate/100)
//tax = salePrice * 0.65
//TotalPrice = salePrice + Tax

// "Enter the original price" 
// inputs: originalPrice 

// "Enter the discount rate"
// input: discountRate

// formula : calculate sale price:                                          //	salePrice = originalPrice - AmountSaved
//  SET salePrice
//	to originalPrice - amountSaved


	
	
	// variables taxable income and taxDue
	//	 

/*
 * 
 * Suppose you want to write a program that will input a person’s weight in pounds and output that
 * weight in kilograms. Analyze this problem: give the input and output variables, as well as the 
 * formula that will produce the required output from the given input.
 * 	 (Hint: the necessary formula is P = 2.2046 * K, where P is the number of pounds and K is kilograms.)
 * 
 */

	
	
}
